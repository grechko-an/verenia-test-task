import { RepositoriesListFilterPipe } from './repositories-list-filter.pipe';

describe('RepositoriesListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RepositoriesListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
